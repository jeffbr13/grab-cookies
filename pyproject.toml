[project]
name = "grab-cookies"
version = "0.1.0"
description = "Extract browser cookies into a Netscape-format cookies.txt file"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "yt-dlp>=2025.01.01",
]

[project.scripts]
grab-cookies = "main:main"
